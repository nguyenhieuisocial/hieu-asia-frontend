fix(cta): guard quét cả object-literal — bắt thêm 7 chỗ nhãn hứa công cụ nhưng trỏ nơi khác

Đào tiếp sau #1006. Hai phát hiện:

1. AUDIT TRANG MỒ CÔI — kết quả sạch, ghi lại để khỏi ai đào lại:
   quét 224 route ↔ inbound từ crawl 325 trang + sitemap 978 URL. 29 route
   tĩnh + 10 động có 0 inbound nhưng KIỂM TỪNG CÁI đều là chủ đích: khu
   /account (sau đăng nhập), bước wizard, route hệ thống, và 4 redirect có
   ghi chú đầy đủ (/dashboard, /settings, /onboarding-wizard,
   /checkout/premium). Hai route động tưởng mồ côi (/cung-hoang-dao/hop/
   [pair], /gio-hoang-dao/y-nghia/[slug]) thật ra được hub link đầy đủ —
   chỉ nằm sâu hơn tầm crawl. KHÔNG có trang mồ côi thật.

2. LỖI THẬT — tryCta của 3 trang learn hứa một đằng dẫn một nẻo:
   blurb hứa "30 giây, xem đầy đủ trước khi mở khoá, không cần đăng ký"
   (= công cụ lá số tức thì) nhưng href = /reading/new?method=X → 307 vào
   wizard /onboarding/birth (consent + chờ server 30-60s + paywall cuối
   đường). Route /reading/new tự ghi chú: giữ cho back-compat link CŨ và
   tham số ?method= bị bỏ khi redirect — 3 CTA nội bộ này lẽ ra phải được
   cập nhật từ Wave 60.58. → trỏ thẳng /la-so-tu-vi, /la-so-bat-tu.

   Guard #1006 KHÔNG bắt được vụ này vì nhãn+href truyền qua object prop
   (tryCta={{...}}), không phải thẻ <Link>. Mở rộng guard quét cả object
   literal (href↔label hai chiều thứ tự khoá) — chạy lại lập tức bắt thêm
   4 chỗ cùng bệnh trong RelatedTools: "Lập lá số Tử Vi" → /tu-vi (hub)
   ở community/cases + methodology/bat-tu + methodology/tu-vi, và
   "Lập lá số Bát Tự" → /bat-tu ở learn/dat-ten-ngu-hanh. Nhãn hành động
   "Lập lá số..." phải ra công cụ, không phải trang giới thiệu. Sửa cả 4.

   Sau sửa: nhãn "lập lá số tử vi (của bạn)" thống nhất một đích
   /la-so-tu-vi trên toàn site; "lập lá số bát tự" → /la-so-bat-tu.

Cả hai guard (cta-consistency + site-registry) xanh. Build xanh.
